# try-prisma-nextjs

## Backend

copy `variables.env.sample` to `variables.env` and set your parameters.

```shell
cd backend && yarn
yarn deploy
yarn dev
```

## Frontend

```shell
cd frontend && yarn
yarn dev
```
